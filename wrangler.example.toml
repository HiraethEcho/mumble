# 示例配置 — 复制为 wrangler.toml 并填入真实值
# wrangler.toml 已被 .gitignore 忽略（含敏感信息），勿提交
name = "mumble"
main = "worker/index.js"
compatibility_date = "2026-08-06"
assets = { directory = "./public" }

[[d1_databases]]
binding = "DB"
database_name = "mumble-db"
# 替换为真实 database_id：npx wrangler d1 create mumble-db 的输出
database_id = "00000000-0000-0000-0000-000000000000"

[vars]
# 非敏感配置可放这里（前端 config.js 之外的站点配置）
ADMIN_NAME = "admin"   # 管理员昵称

# 敏感配置用 secret 设置，不要放这里：
#   npx wrangler secret put ADMIN_EMAIL     # 管理员邮箱
#   npx wrangler secret put ADMIN_PASSWORD  # 管理员密码
#   npx wrangler secret put SESSION_SECRET  # 会话密钥（随机串，如 openssl rand -hex 32）
